
@using Era_sphere.Areas.AreaHoteles.Models.HotelXServicioXTemporadaNM;
@using Era_sphere.Generics;
@model IEnumerable<HotelXServicioXTemporadaView>
@using Telerik.Web.Mvc.UI

@{
    ViewBag.Title = "IndexHotelXServicioXTemporada";
    Layout = "~/Views/Shared/_Layout.cshtml";
    var hotelID = ViewData["hotelID"];
}


<div class="block big" style="position: relative">
    <!-- Block Begin -->
    <div class="titlebar">
        <h3>
            Precios de servicios
        </h3>
    </div>
    <div class="block_cont">
        <p> Hotel @ViewData["hotel"] </p>   
        @(Html.Telerik().Grid(Model)
        .Name("HotelXServicioXTemporadaGrid")
        .Localizable("es-ES")
        .Filterable()
        .ToolBar(commands => commands.Insert())
        .Editable(editing => editing
                            .Mode(GridEditMode.PopUp)
                            .TemplateName("HotelXServicioXTemporadaViewTemplate"))

        .DataKeys(key => key.Add(v => v.ID))
        .Columns(columns =>
        {
            columns.Bound(v => v.servicio_descripcion);
            //columns.Bound(v => v.tt_desc);
            columns.Bound(v => v.t_desc);
            columns.Bound(v => v.precio);
            columns.Command(commands =>
            {
                commands.Edit().ButtonType(GridButtonType.Image);
                commands.Delete().ButtonType(GridButtonType.Image);
            });
        })

        .Pageable()
        .Sortable()


        .DataBinding(dataBinding => dataBinding
            .Ajax()
            .Select("Select", "HotelXServicioXTemporada", new { id = @hotelID })
            .Insert("Insert", "HotelXServicioXTemporada", new { id = @hotelID })
            .Delete("Delete", "HotelXServicioXTemporada", new { id_hotel = @hotelID })
            .Update("Update", "HotelXServicioXTemporada", new { id_hotel = @hotelID })
        )
           )

    </div>
    <!-- Table Wrapper End -->
</div>